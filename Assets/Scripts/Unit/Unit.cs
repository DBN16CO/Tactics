using UnityEngine;

// Class governing unit options
public class Unit : MonoBehaviour {

	// Called from the clicked token when there is a unit
	public void Clicked() {
		// UI Options if the unit is yours and hasn't taken its turn
		Debug.Log(name);
		// UI Options if the unit is yours and has taken its turn

		// UI Options if the unit isn't yours

	}

}
